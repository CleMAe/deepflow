import { Card } from 'antd'

export default function ProjectAgentsPage() {
  return (
    <div>
      <h2 style={{ marginBottom: 24 }}>Agent 管理</h2>
      <Card>
        <p>Agent 列表 / 对话 / 工具绑定（P5 待实现）</p>
      </Card>
    </div>
  )
}
